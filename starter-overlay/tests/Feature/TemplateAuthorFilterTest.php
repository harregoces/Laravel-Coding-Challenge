<?php

namespace Tests\Feature;

use Tests\TestCase;
use Illuminate\Foundation\Testing\RefreshDatabase;

/**
 * Template test for the optional `?author=` filter enhancement.
 * This test is intentionally marked incomplete so it does not affect grading.
 * Candidates may replace it with real assertions if they implement the feature.
 */
class TemplateAuthorFilterTest extends TestCase
{
    use RefreshDatabase;

    public function test_author_filter_example_template(): void
    {
        $this->markTestIncomplete('Optional: implement ?author= filter and write real assertions here.');
    }
}
